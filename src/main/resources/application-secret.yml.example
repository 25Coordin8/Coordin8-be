# 데이터베이스 연결 설정 예시 파일
# 이 파일을 복사하여 application-secret.yml로 이름을 변경하고 실제 값으로 수정하세요.
# 
# 사용 방법:
# 1. 이 파일을 복사: cp application-secret.yml.example application-secret.yml
# 2. 아래 값들을 실제 데이터베이스 정보로 수정
# 3. Git에 커밋하지 않도록 주의 (이미 .gitignore에 추가됨)

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/coordin8
    username: root
    password: your_password_here
    driver-class-name: org.mariadb.jdbc.Driver

# 또는 환경 변수 사용 (권장):
# spring:
#   datasource:
#     url: ${DB_URL:jdbc:mariadb://localhost:3306/coordin8}
#     username: ${DB_USERNAME:root}
#     password: ${DB_PASSWORD:your_password_here}
#     driver-class-name: org.mariadb.jdbc.Driver

